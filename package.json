{
  "name": "root",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "dev": "docker compose up --remove-orphans",
    "start": "sh ./scripts/kubernetes-sh.sh",
    "start:workers": "pnpm --filter=workers exec -- deno task kubernetes",
    "start:astro": "pnpm --filter=astro start:server",
    "build:astro": "pnpm --filter=astro build",
    "dev:astro": "pnpm --filter=astro dev",
    "kompose": "kompose convert --out ./k8s/kind.yaml",
    "docker": "pnpm docker:astro && pnpm docker:workers",
    "docker:workers": "docker build -t skf/workers -f ./packages/workers/Dockerfile --target prod .",
    "docker:astro": "docker build -t skf/astro -f ./packages/astro/Dockerfile --target prod ."
  },
  "devDependencies": {
    "typescript": "^4.9.5"
  }
}